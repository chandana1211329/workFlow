# MongoDB Cluster Configuration
MONGODB_URI=mongodb://app_user:app_password123@localhost:27017,localhost:27018,localhost:27019/document-generator?replicaSet=rs0&authSource=document-generator

# Alternative connection strings (choose one):
# For primary node only:
# MONGODB_URI=mongodb://app_user:app_password123@localhost:27017/document-generator?authSource=document-generator

# For admin access:
# MONGODB_URI=mongodb://admin:admin123@localhost:27017,localhost:27018,localhost:27019/document-generator?replicaSet=rs0&authSource=admin

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Email Configuration (update with your actual email settings)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
MANAGER_EMAIL=manager@yourcompany.com
